/** Phase 0 placeholder. The real detail page (plan §8.3) ships in Phase 1. */
import { LibraryBig } from 'lucide-react';

import { ComingSoonPage } from '@/features/comingSoon/ComingSoonPage';

export function WaifumonDetailPage() {
  return (
    <ComingSoonPage
      title="Waifumon"
      description="A single copy from your collection."
      icon={LibraryBig}
      detail="Scaffolding is in place; the detail page lands in Phase 1."
    />
  );
}
